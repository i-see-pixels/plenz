name = "promptlens"
type = "nextjs"

[build]
  command = "pnpm install && pnpm build"
  cwd = "apps/web"

[env]
  NODE_ENV = "production"

[routes]
  # Route for the Next.js app
  route = "*"
  pattern = "/*"